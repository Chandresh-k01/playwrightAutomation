# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Logout.spec.js >> TC 1 - Login and Logout
- Location: tests\Logout.spec.js:7:5

# Error details

```
TypeError: Cannot read properties of undefined (reading '0')
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | import logoutData from '../testdata/LogOut.json';
  4  | import LoginPage from '../pages/LoginPage';
  5  | import Logoutpage from '../pages/Logoutpage';
  6  | 
  7  | test('TC 1 - Login and Logout', async ({ page }) => {
  8  | 
  9  |     const login = new LoginPage(page);
  10 |     const logout = new Logoutpage(page);
  11 | 
> 12 |     const data = logoutData.logoutTests[0];
     |                                        ^ TypeError: Cannot read properties of undefined (reading '0')
  13 | 
  14 |     // Open Application
  15 |     await login.gotoURL();
  16 | 
  17 |     // Login
  18 |     await login.login(
  19 |         data.username,
  20 |         data.password
  21 |     );
  22 | 
  23 |     // Verify Login
  24 |     await expect(login.message.first()).toHaveText(data.expected);
  25 | 
  26 |     // Open Menu
  27 |     await logout.openMenu();
  28 | 
  29 |     // Logout
  30 |     await logout.logout();
  31 | 
  32 |     // Verify Logout
  33 |     await expect(logout.loginButton).toBeVisible();
  34 | 
  35 | });
```